[project]
name = "days-since"
version = "0.1.0"
description = "Track days elapsed since specific dates"
readme = "README.md"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
